import{createClient}from"@supabase/supabase-js";const url=process.env.LOCAL_SUPABASE_URL,key=process.env.LOCAL_SUPABASE_SERVICE_ROLE_KEY;if(!url?.includes("127.0.0.1")||!key)throw new Error("Local Supabase required");const s=createClient(url,key,{auth:{persistSession:false}}),{data:orgs}=await s.from("organizations").select("id").like("slug","p3c-%"),ids=(orgs??[]).map(x=>x.id);if(ids.length){for(const table of["supplier_issues","quality_documents","qc_hold_decisions","qc_release_events","qc_inspections","receiving_lines","receivings","inventory_transactions","inventory_balances","inventory_lot_sources","inventory_reservations","inventory_lots","inbound_shipments","purchase_order_lines","purchase_orders","warehouse_locations","warehouses","supplier_product_variant_costs","supplier_product_variants","suppliers","product_variants","products","product_categories","organization_memberships"]){await s.from(table).delete().in("organization_id",ids)}await s.from("organizations").delete().in("id",ids)}const{data:{users}}=await s.auth.admin.listUsers({perPage:1000});for(const u of users.filter(x=>x.email?.startsWith("p3c-")))await s.auth.admin.deleteUser(u.id);
await s.from("roles").delete().like("code","P3C_TEST_%");
