import Link from "next/link"; import { CatalogForm } from "@/components/catalog/catalog-form"; import { CatalogList } from "@/components/catalog/catalog-list"; import { PageHeading } from "@/components/ui/page-heading"; import { can,requirePermission } from "@/lib/auth/authorization"; import { saveProduct } from "@/lib/catalog/actions"; import { createClient } from "@/lib/supabase/server";
export default async function Page(){const c=await requirePermission("products.view"),s=await createClient();const [{data:products},{data:categories}]=await Promise.all([s.from("products").select("id,product_name,product_type,status,default_unit_of_measure").order("product_name"),s.from("product_categories").select("id,name").eq("status","active").order("display_order")]);return <><PageHeading title="Products" description={`Master catalog for ${c.membership!.organizationName}.`}/><nav className="mt-4 flex gap-4 text-sm font-semibold"><Link href="/products/categories">Categories</Link><Link href="/products/variants">Variants / SKUs</Link><Link href="/suppliers">Suppliers</Link></nav>{can(c,"products.manage")&&categories?.length?<div className="mt-8"><CatalogForm title="Create product" action={saveProduct} fields={[{name:"categoryId",label:"Category",required:true,options:categories.map(x=>({value:x.id,label:x.name}))},{name:"name",label:"Product name",required:true},{name:"type",label:"Product type",required:true},{name:"uom",label:"Default unit",required:true},{name:"status",label:"Status",options:[{value:"active",label:"Active"},{value:"inactive",label:"Inactive"},{value:"depleted",label:"Depleted"},{value:"discontinued",label:"Discontinued"}]},{name:"description",label:"Description"},{name:"notes",label:"Internal notes"},{name:"lot",label:"Lot tracking",type:"checkbox"},{name:"expiration",label:"Expiration/BUD tracking",type:"checkbox"},{name:"coa",label:"COA applicable",type:"checkbox"},{name:"backorder",label:"Future backorder eligibility",type:"checkbox"}]}/></div>:null}<CatalogList title="Master products" rows={products??[]}/></>}
