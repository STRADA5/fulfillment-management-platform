import{CatalogForm}from"@/components/catalog/catalog-form";import{CatalogList}from"@/components/catalog/catalog-list";import{PageHeading}from"@/components/ui/page-heading";import{can,requirePermission}from"@/lib/auth/authorization";import{saveCategory}from"@/lib/catalog/actions";import{createClient}from"@/lib/supabase/server";
export default async function Page(){const c=await requirePermission("products.view"),s=await createClient(),{data}=await s.from("product_categories").select("id,name,slug,status,display_order").order("display_order");return <><PageHeading title="Product Categories" description={`Flexible category definitions for ${c.membership!.organizationName}.`}/>{can(c,"categories.manage")?<div className="mt-8"><CatalogForm title="Create category" action={saveCategory} fields={[{name:"name",label:"Name",required:true},{name:"slug",label:"Slug",required:true},{name:"description",label:"Description"},{name:"displayOrder",label:"Display order",type:"number",required:true},{name:"status",label:"Status",options:[{value:"active",label:"Active"},{value:"inactive",label:"Inactive"}]}]}/></div>:null}<CatalogList title="Categories" rows={data??[]}/></>}
