import{CatalogForm}from"@/components/catalog/catalog-form";import{CatalogList}from"@/components/catalog/catalog-list";import{PageHeading}from"@/components/ui/page-heading";import{can,requirePermission}from"@/lib/auth/authorization";import{saveVariant}from"@/lib/catalog/actions";import{createClient}from"@/lib/supabase/server";
export default async function Page(){const c=await requirePermission("products.view"),s=await createClient();const[{data:rows},{data:products}]=await Promise.all([s.from("product_variants").select("id,sku,variant_name,strength_value,strength_unit,package_size,package_unit,status").order("sku"),s.from("products").select("id,product_name").order("product_name")]);return <><PageHeading title="Product Variants / SKUs" description={`Sellable configurations for ${c.membership!.organizationName}.`}/>{can(c,"products.manage")&&products?.length?<div className="mt-8"><CatalogForm title="Create variant" action={saveVariant} fields={[{name:"productId",label:"Product",required:true,options:products.map(x=>({value:x.id,label:x.product_name}))},{name:"sku",label:"SKU",required:true},{name:"name",label:"Variant name",required:true},{name:"strength",label:"Strength",type:"number"},{name:"strengthUnit",label:"Strength unit"},{name:"packageSize",label:"Package size",type:"number"},{name:"packageUnit",label:"Package unit"},{name:"concentration",label:"Concentration"},{name:"volume",label:"Volume",type:"number"},{name:"volumeUnit",label:"Volume unit"},{name:"barcode",label:"Barcode"},{name:"status",label:"Status",options:[{value:"active",label:"Active"},{value:"inactive",label:"Inactive"}]}]}/></div>:null}<CatalogList title="Variants" rows={rows??[]}/></>}
