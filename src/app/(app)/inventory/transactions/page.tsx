import{CatalogList}from"@/components/catalog/catalog-list";import{PageHeading}from"@/components/ui/page-heading";import{requirePermission}from"@/lib/auth/authorization";import{createClient}from"@/lib/supabase/server";
export default async function Page(){await requirePermission("inventory.view");const s=await createClient(),{data}=await s.from("inventory_transactions").select("id,created_at,transaction_type,physical_delta,available_delta,reserved_delta,quarantined_delta,damaged_delta,reference_type,reference_id,reason,warehouse_id,location_id,lot_id,actor_user_id").order("created_at",{ascending:false}).limit(250);return <><PageHeading title="Inventory Transactions" description="Immutable ledger history explaining every stock change."/><CatalogList title="Transaction history" rows={data??[]}/></>}
