import Link from"next/link";import{CatalogForm}from"@/components/catalog/catalog-form";import{CatalogList}from"@/components/catalog/catalog-list";import{PageHeading}from"@/components/ui/page-heading";import{can,requirePermission}from"@/lib/auth/authorization";import{saveWarehouse}from"@/lib/inventory/actions";import{createClient}from"@/lib/supabase/server";
export default async function Page(){const c=await requirePermission("warehouses.view"),s=await createClient(),{data}=await s.from("warehouses").select("id,warehouse_code,warehouse_name,status,city,region,country,timezone").order("warehouse_code");return <><PageHeading title="Warehouses" description={`Internal warehouse network for ${c.membership!.organizationName}.`}/><Link href="/warehouses/locations" className="mt-4 inline-block font-semibold">Warehouse Locations</Link>{can(c,"warehouses.manage")?<div className="mt-8"><CatalogForm title="Create warehouse" action={saveWarehouse} fields={[{name:"code",label:"Warehouse code",required:true},{name:"name",label:"Warehouse name",required:true},{name:"status",label:"Status",options:[{value:"active",label:"Active"},{value:"inactive",label:"Inactive"},{value:"temporarily_closed",label:"Temporarily closed"}]},{name:"city",label:"City"},{name:"region",label:"State / province"},{name:"postal",label:"Postal code"},{name:"country",label:"Country",required:true},{name:"timezone",label:"Timezone",required:true},{name:"notes",label:"Internal notes"}]}/></div>:null}<CatalogList title="Warehouses" rows={data??[]} editKind="warehouse"/></>}
