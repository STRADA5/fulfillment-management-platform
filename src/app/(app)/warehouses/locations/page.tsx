import{CatalogForm}from"@/components/catalog/catalog-form";import{CatalogList}from"@/components/catalog/catalog-list";import{PageHeading}from"@/components/ui/page-heading";import{can,requirePermission}from"@/lib/auth/authorization";import{saveLocation}from"@/lib/inventory/actions";import{createClient}from"@/lib/supabase/server";
export default async function Page(){const c=await requirePermission("warehouses.view"),s=await createClient();const[{data:rows},{data:warehouses}]=await Promise.all([s.from("warehouse_locations").select("id,warehouse_id,location_code,location_name,location_type,status").order("location_code"),s.from("warehouses").select("id,warehouse_name").order("warehouse_name")]);return <><PageHeading title="Warehouse Locations" description="Configurable receiving, quarantine, storage, staging, packing, and other locations."/>{can(c,"warehouses.manage")&&warehouses?.length?<div className="mt-8"><CatalogForm title="Create location" action={saveLocation} fields={[{name:"warehouseId",label:"Warehouse",required:true,options:warehouses.map(x=>({value:x.id,label:x.warehouse_name}))},{name:"code",label:"Location code",required:true},{name:"name",label:"Location name",required:true},{name:"type",label:"Location type",required:true},{name:"status",label:"Status",options:[{value:"active",label:"Active"},{value:"inactive",label:"Inactive"}]},{name:"description",label:"Description"},{name:"notes",label:"Internal notes"}]}/></div>:null}<CatalogList title="Warehouse locations" rows={rows??[]} editKind="location"/></>}
